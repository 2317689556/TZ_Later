<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.dao.UserMapper">
    <!--   登录 -->
    <select id="login" parameterType="User" resultType="User">
        SELECT *
        FROM user
        WHERE username=#{username}
        AND password=#{password}
    </select>

    <!--   登录 -->
    <select id="login1" resultType="java.lang.String">
        SELECT *
        FROM user
        WHERE username=#{username}
        AND password=#{password}
    </select>

    <!--轮播图查询-->
    <!--List<AoyoCommodityImg> lunbo();-->
    <select id="lunbo" resultType="aoyoCommodityImg">
        select commodity_img_url from aoyo_commodity_img
    </select>



</mapper>
